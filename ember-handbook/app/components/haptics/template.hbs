<Haptics::Matrix::Table>
  <thead>
    <Haptics::Matrix::Head />
  </thead>
  <tbody>
    <Haptics::Matrix::Row
      @element={{@element}}
      @styled={{@styled}}
      @content={{@content}}
      @structure={{@structure}}
      @yields={{@yields}}
      @logic={{@logic}}
    />
  </tbody>
</Haptics::Matrix::Table>