<Haptics::Matrix::Table>
  <thead>
    <Haptics::Matrix::Head />
  </thead>
  <tbody>
    <Haptics::Matrix::Row
      @invocation={{@invocation}}
      @styled={{@styled}}
      @content={{@content}}
      @structure={{@structure}}
      @logic={{@logic}}
    />
  </tbody>
</Haptics::Matrix::Table>